import { ErcDex } from '@ercdex/core';
import { KeyService } from './services/key-service';

export const config = {
  network: undefined as any as ErcDex.Api.INetwork,
  keyService: undefined as any as KeyService
};
